
name: CI
on:
  push:
    branches:
      - main
  schedule:
    - cron: '0 * * * *'


jobs:
  build:
    runs-on: ubuntu-latest

    steps:
      - uses: actions/checkout@v2

      - shell: bash
        run: |
          curl -s https://coronavirus-tracker-api.herokuapp.com/v2/latest | jq
